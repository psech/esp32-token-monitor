# Local-dev overrides. Copy to .env and adjust.
# Docker compose ignores this file — see docker-compose.yml for container env.
PORT=8787
CREDENTIALS_FILE=./.secrets/credentials.json
NODE_EXTRA_CA_CERTS=/usr/local/share/netskope-cert-bundle.pem
UPSTREAM_CACHE_TTL_MS=60000
LOG_LEVEL=info
